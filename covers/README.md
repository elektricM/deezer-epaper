Benchmark fixtures for `tools/benchmark.py`, spanning dark, bright, neutral,
skin and specific hues (cyan, magenta, orange, blue).

The images are not in the repository - they are album art. Run

    python3 covers/fetch.py

to pull them from Deezer's public catalogue for local benchmarking.
